export const environment = {
  production: true,
  // Empty apiUrl assumes the API is served from the same origin (e.g. behind a
  // reverse proxy). Set this to the API's origin if it's hosted separately.
  apiUrl: ''
};
